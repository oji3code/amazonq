#==========================================================
# Helm install
#==========================================================

helm repo add datadog https://helm.datadoghq.com
helm repo update

kubectl create secret generic datadog-secret --from-literal api-key=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

cat << EOF > datadog-values.yaml
datadog:
  site: "ap1.datadoghq.com"
  apiKeyExistingSecret: "datadog-secret"
  logs:
    enabled: true
    containerCollectAll: true
EOF

helm install datadog-agent -f datadog-values.yaml datadog/datadog

#==========================================================
# Create Namespace
#==========================================================

cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: Namespace
metadata:
  name: karpenter-node-logger
EOF

#==========================================================
# Create SA, Role, ClusterRoleBinding
#==========================================================

cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: ServiceAccount
metadata:
  name: karpenter-node-logger
  namespace: karpenter-node-logger
EOF

cat <<EOF | kubectl apply -f -
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: karpenter-node-logger # 任意でわかりやすい名前に変更可能
rules:
- apiGroups: [""] # "" はコアAPIグループ（ノードなど）を示す
  resources: ["nodes"]
  verbs: ["get", "list", "watch"] # get だけでなく、list と watch もあると良い
- apiGroups: ["karpenter.sh"] # NodeClaimリソースのAPIグループ
  resources: ["nodeclaims"] # 監視したいリソース
  verbs: ["get", "list", "watch"] # 取得、一覧表示、監視の権限を付与
EOF

cat <<EOF | kubectl apply -f -
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: karpenter-node-logger-binding # 任意でわかりやすい名前に変更可能
subjects:
- kind: ServiceAccount
  name: karpenter-node-logger # エラーメッセージに表示されたサービスアカウント名
  namespace: karpenter-node-logger # エラーメッセージに表示されたサービスアカウントの名前空間
roleRef:
  kind: ClusterRole
  name: karpenter-node-logger # 上記で作成したClusterRoleの名前
  apiGroup: rbac.authorization.k8s.io
EOF

#==========================================================
# Create Deployment(Python App: Monitor for Kubernetes's Event of Node)
#==========================================================

kubectl delete deployment karpenter-node-logger -n karpenter-node-logger
cat <<EOF | kubectl apply -f -
apiVersion: apps/v1
kind: Deployment
metadata:
  name: karpenter-node-logger
  namespace: karpenter-node-logger
spec:
  replicas: 1
  selector:
    matchLabels:
      app: karpenter-node-logger
  template:
    metadata:
      labels:
        app: karpenter-node-logger
      annotations:
        ad.datadoghq.com/karpenter-node-logger.logs: '[{"source": "python", "service": "karpenter", "env": "inf"}]'
    spec:
      serviceAccountName: karpenter-node-logger
      containers:
      - name: karpenter-node-logger
        image: python:3.9-slim
        command:
        - /bin/bash
        - -c
        - |
          pip install kubernetes requests python-json-logger > /dev/null 2>&1
          mkdir -p /app
          cat > /app/karpenter-node-logger.py << EOF_PYTHON
          import logging
          from pythonjsonlogger import jsonlogger
          from kubernetes import client, config, watch
          import os
          
          # ログ出力フォーマットをJSON形式で定義
          LOG_FORMAT = '''
            {
              "timestamp": "%(asctime)s",
              "level": "%(levelname)s",
              "message": "%(message)s",
              "nodeclaim_name": "%(nodeclaim_name)s",
              "node_name": "%(node_name)s",
              "instance_id": "%(instance_id)s",
              "node_ip_address": "%(node_ip_address)s",
              "event_type": "%(event_type)s",
              "event_status": "%(event_status)s",
              "scale_action": "%(scale_action)s"
            }
          '''
          
          # INFOレベル以上のログのみ出力
          log_level = os.getenv('LOG_LEVEL', 'INFO').upper()
          logger = logging.getLogger('karpenter-node-logger')
          logger.setLevel(log_level)
          
          logHandler = logging.StreamHandler()
          formatter = jsonlogger.JsonFormatter(LOG_FORMAT)
          logHandler.setFormatter(formatter)
          logger.addHandler(logHandler)
          
          # ログにカスタムフィールドを追加するためのアダプタークラス
          class CustomLogAdapter(logging.LoggerAdapter):
            def process(self, msg, kwargs):
              extra_fields = {
                "nodeclaim_name": kwargs.pop("nodeclaim_name", "N/A"),
                "node_name": kwargs.pop("node_name", "N/A"),
                "instance_id": kwargs.pop("instance_id", "N/A"),
                "node_ip_address": kwargs.pop("node_ip_address", "N/A"),
                "event_type": kwargs.pop("event_type", "N/A"),
              }
              if 'extra' in kwargs:
                  kwargs['extra'].update(extra_fields)
              else:
                  kwargs['extra'] = extra_fields
              return msg, kwargs
          
          karpenter_logger = CustomLogAdapter(logger, {})
          
          # 最後にログに記録されたNodeの状態を保持する辞書
          last_logged_node_states = {}
          
          # Kubernetes NodeからInternal IPアドレスを取得する関数
          def get_node_info(core_api, node_name):
            node_ip_address = "N/A"
            if node_name and node_name != "N/A":
              try:
                node_obj = core_api.read_node(name=node_name)
                for address in node_obj.status.addresses:
                  if address.type == 'InternalIP':
                    node_ip_address = address.address
                    break
              except client.ApiException as e:
                # Nodeがまだ登録されていない、またはAPIエラーの場合でも、ここではログを出さない
                pass 
            return node_ip_address
          
          # Karpenterノードのスケーリングイベントを監視してログに記録するメイン関数
          def logging_karpenter_node_scale():
            try:
              config.load_kube_config()
            except config.ConfigException:
              # 設定ファイルがない場合でも、ここではログを出さない
              config.load_incluster_config()
          
            core_api = client.CoreV1Api()
            custom_api = client.CustomObjectsApi()
            w = watch.Watch()
          
            # 監視開始のログも出力しない
          
            # NodeClaimカスタムリソースの変更をストリームで監視
            for event in w.stream(custom_api.list_cluster_custom_object,
                                    group="karpenter.sh",
                                    version="v1",
                                    plural="nodeclaims"):
              
              nodeclaim = event['object']
              original_event_type = event['type'] # イベントタイプ (ADDED, MODIFIED, DELETED)
              nodeclaim_name = nodeclaim['metadata']['name']
              
              # NodeClaimからnodeNameとproviderID（インスタンスID）を取得
              node_name_from_nodeclaim = nodeclaim.get('status', {}).get('nodeName', 'N/A')
              instance_id_from_nodeclaim = "N/A"
              provider_id = nodeclaim.get('status', {}).get('providerID')
              if provider_id:
                instance_id_from_nodeclaim = provider_id.split('/')[-1]
          
              is_ready = False
              # NodeClaimのReady状態を確認
              for condition in nodeclaim.get('status', {}).get('conditions', []):
                if condition.get('type') == 'Ready' and condition.get('status') == 'True':
                  is_ready = True
          
              # 前回のログ記録時のNodeの状態を取得または初期化
              current_node_state = last_logged_node_states.get(nodeclaim_name, {
                  "is_ready": False,
                  "deleted_logged": False,
                  "cached_nodeclaim_name": nodeclaim_name,
                  "cached_node_name": "N/A",
                  "cached_instance_id": "N/A",
                  "cached_node_ip_address": "N/A"
              })
          
              # イベントタイプに応じたログ処理
              if original_event_type == 'MODIFIED':
                # NodeClaim変更イベント
                if is_ready and not current_node_state["is_ready"]:
                  # 初めてReadyになった時にNode情報を取得
                  actual_node_ip_address = get_node_info(core_api, node_name_from_nodeclaim)
                  
                  # Node名、インスタンスID、IPアドレスがすべて"N/A"ではない場合にのみキャッシュし、INFOログを出力
                  if node_name_from_nodeclaim != "N/A" and \
                     instance_id_from_nodeclaim != "N/A" and \
                     actual_node_ip_address != "N/A":
                    
                    # キャッシュを更新
                    current_node_state["is_ready"] = True
                    current_node_state["cached_node_name"] = node_name_from_nodeclaim
                    current_node_state["cached_instance_id"] = instance_id_from_nodeclaim
                    current_node_state["cached_node_ip_address"] = actual_node_ip_address
                    last_logged_node_states[nodeclaim_name] = current_node_state
          
                    # ReadyになったNode情報をInfoレベルでログに出力 (スケールアウト完了)
                    karpenter_logger.info(f"Node is READY and joined the cluster. Scale Out completed.",
                                           nodeclaim_name=current_node_state["cached_nodeclaim_name"],
                                           node_name=current_node_state["cached_node_name"],
                                           instance_id=current_node_state["cached_instance_id"],
                                           node_ip_address=current_node_state["cached_node_ip_address"],
                                           event_type=original_event_type,
                                           extra={"event_status": "node_ready", "scale_action": "ScaleOut"})
          
              elif original_event_type == 'DELETED':
                # NodeClaim削除イベント (スケールイン完了)
                # キャッシュされた情報がある場合にのみログを出力
                if nodeclaim_name in last_logged_node_states and not current_node_state["deleted_logged"]:
                  # DELETEDイベントでは常にキャッシュされた情報を使用
                  karpenter_logger.info(f"NodeClaim DELETED. Node has been terminated. Scale In completed.",
                                         nodeclaim_name=current_node_state["cached_nodeclaim_name"],
                                         node_name=current_node_state["cached_node_name"],
                                         instance_id=current_node_state["cached_instance_id"],
                                         node_ip_address=current_node_state["cached_node_ip_address"],
                                         event_type=original_event_type,
                                         extra={"event_status": "scale_in_terminated", "scale_action": "ScaleIn"})
                  current_node_state["deleted_logged"] = True
                  # ログ記録後、キャッシュから削除
                  del last_logged_node_states[nodeclaim_name]
                # キャッシュがない、または既にログ済みの場合、何も出力しない
                            
          if __name__ == "__main__":
            logging_karpenter_node_scale()
          EOF_PYTHON
          python /app/karpenter-node-logger.py
        env:
        - name: LOG_LEVEL
          value: INFO
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
EOF

#==========================================================
# Verify Pod
#==========================================================

kubectl get pods -n karpenter-node-logger

kubectl logs -n karpenter-node-logger $(kubectl get po -n karpenter-node-logger -o jsonpath={.items[0].metadata.name}) -f
