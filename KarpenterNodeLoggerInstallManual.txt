#==========================================================
# Helm install
#==========================================================

helm repo add datadog https://helm.datadoghq.com
helm repo update

kubectl create secret generic datadog-secret --from-literal api-key=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

cat << EOF > datadog-values.yaml
datadog:
  site: "ap1.datadoghq.com"
  apiKeyExistingSecret: "datadog-secret"
  logs:
    enabled: true
    containerCollectAll: true
EOF

helm install datadog-agent -f datadog-values.yaml datadog/datadog

#==========================================================
# Create Namespace
#==========================================================

cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: Namespace
metadata:
  name: karpenter-scale-logger
EOF

#==========================================================
# Create SA, Role, ClusterRoleBinding
#==========================================================

cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: ServiceAccount
metadata:
  name: karpenter-scale-logger
  namespace: karpenter-scale-logger
EOF

cat <<EOF | kubectl apply -f -
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: karpenter-scale-logger
rules:
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["karpenter.sh"]
  resources: ["nodeclaims"]
  verbs: ["get", "list", "watch"]
EOF

cat <<EOF | kubectl apply -f -
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: karpenter-scale-logger-binding
subjects:
- kind: ServiceAccount
  name: karpenter-scale-logger
  namespace: karpenter-scale-logger
roleRef:
  kind: ClusterRole
  name: karpenter-scale-logger
  apiGroup: rbac.authorization.k8s.io
EOF

#==========================================================
# Create ConfigMap
#==========================================================
export ENV="inf"
cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: ConfigMap
metadata:
  name: karpenter-scale-logger-config
  namespace: karpenter-scale-logger
data:
  ENV: "${ENV}"
  LOG_LEVEL: "INFO"
EOF

#==========================================================
# Create Deployment(Python App: Monitor for Kubernetes's Event of Node)
#==========================================================

kubectl delete deployment karpenter-scale-logger -n karpenter-scale-logger
cat <<EOF | kubectl apply -f -
apiVersion: apps/v1
kind: Deployment
metadata:
  name: karpenter-scale-logger
  namespace: karpenter-scale-logger
spec:
  replicas: 1
  selector:
    matchLabels:
      app: karpenter-scale-logger
  template:
    metadata:
      labels:
        app: karpenter-scale-logger
      annotations:
        ad.datadoghq.com/karpenter-scale-logger.logs: '[{"source": "python", "service": "karpenter"]'
    spec:
      serviceAccountName: karpenter-scale-logger
      containers:
      - name: karpenter-scale-logger
        image: python:3.9-slim
        command:
        - /bin/bash
        - -c
        - |
          pip install kubernetes requests python-json-logger > /dev/null 2>&1
          mkdir -p /app
          cat > /app/karpenter-scale-logger.py << EOF_PYTHON
          import logging
          from pythonjsonlogger import jsonlogger
          from kubernetes import client, config, watch
          import os
          
          # JSONログ出力フォーマットを定義
          LOG_FORMAT = '''
            {
              "timestamp": "%(asctime)s",
              "level": "%(levelname)s",
              "message": "%(message)s",
              "env": "%(env)s",
              "nodeclaim_name": "%(nodeclaim_name)s",
              "node_name": "%(node_name)s",
              "instance_id": "%(instance_id)s",
              "node_ip_address": "%(node_ip_address)s",
              "event_type": "%(event_type)s",
              "event_status": "%(event_status)s",
              "scale_action": "%(scale_action)s"
            }
          '''
          
          # ログレベルを設定
          log_level = os.getenv('LOG_LEVEL', 'INFO').upper()
          logger = logging.getLogger('karpenter-scale-logger')
          logger.setLevel(log_level)
          
          logHandler = logging.StreamHandler()
          formatter = jsonlogger.JsonFormatter(LOG_FORMAT)
          logHandler.setFormatter(formatter)
          logger.addHandler(logHandler)
          
          # カスタムフィールドをログに追加するためのアダプター
          class CustomLogAdapter(logging.LoggerAdapter):
            def process(self, msg, kwargs):
              extra_fields = {
                "env": os.getenv('ENV', 'N/A'),
                "nodeclaim_name": kwargs.pop("nodeclaim_name", "N/A"),
                "node_name": kwargs.pop("node_name", "N/A"),
                "instance_id": kwargs.pop("instance_id", "N/A"),
                "node_ip_address": kwargs.pop("node_ip_address", "N/A"),
                "event_type": kwargs.pop("event_type", "N/A")
              }
              if 'extra' in kwargs:
                kwargs['extra'].update(extra_fields)
              else:
                kwargs['extra'] = extra_fields
              return msg, kwargs
          
          karpenter_logger = CustomLogAdapter(logger, {})
          
          # 最後にログに記録されたNodeの状態を保持する辞書
          last_logged_node_states = {}
          
          # Kubernetes NodeからInternal IPアドレスを取得
          def get_node_info(core_api, node_name):
            node_ip_address = "N/A"
            if node_name and node_name != "N/A":
              try:
                node_obj = core_api.read_node(name=node_name)
                for address in node_obj.status.addresses:
                  if address.type == 'InternalIP':
                    node_ip_address = address.address
                    break
              except client.ApiException as e:
                karpenter_logger.debug(f"Failed to get Node details for {node_name}: {e}. Node might be deleted or not yet registered.",
                                    node_name=node_name,
                                    extra={"event_type": "api_error", "event_status": "debug_node_lookup_failed", "scale_action": "N/A",
                                           "nodeclaim_name": "N/A", "instance_id": "N/A", "node_ip_address": "N/A"})
            return node_ip_address
          
          # Karpenterノードのスケーリングイベントを監視しログに記録
          def logging_karpenter_node_scale():
            try:
              config.load_kube_config()
            except config.ConfigException:
              karpenter_logger.debug("Falling back to in-cluster config for Kubernetes API access.",
                                    nodeclaim_name="system", node_name="system",
                                    instance_id="system", node_ip_address="system",
                                    extra={"event_type": "config_load", "event_status": "debug_in_cluster_config", "scale_action": "N/A"})
              config.load_incluster_config()
          
            core_api = client.CoreV1Api()
            custom_api = client.CustomObjectsApi()
            w = watch.Watch()
          
            karpenter_logger.debug("Starting to watch NodeClaims for events...",
                                    nodeclaim_name="system", node_name="system",
                                    instance_id="system", node_ip_address="system",
                                    extra={"event_type": "watcher_startup", "event_status": "debug_watcher_started", "scale_action": "N/A"})
          
            # NodeClaimカスタムリソースの変更を監視
            for event in w.stream(custom_api.list_cluster_custom_object,
                                    group="karpenter.sh",
                                    version="v1",
                                    plural="nodeclaims"):
              
              nodeclaim = event['object']
              original_event_type = event['type'] 
              nodeclaim_name = nodeclaim['metadata']['name']
              
              node_name_from_nodeclaim = nodeclaim.get('status', {}).get('nodeName', 'N/A')
              instance_id_from_nodeclaim = "N/A"
              provider_id = nodeclaim.get('status', {}).get('providerID')
              if provider_id:
                instance_id_from_nodeclaim = provider_id.split('/')[-1]
          
              is_ready = False
              # NodeClaimのReady状態を確認
              for condition in nodeclaim.get('status', {}).get('conditions', []):
                if condition.get('type') == 'Ready' and condition.get('status') == 'True':
                  is_ready = True
          
              # 前回のログ記録時のNodeの状態を取得または初期化
              current_node_state = last_logged_node_states.get(nodeclaim_name, {
                  "is_ready": False,
                  "deleted_logged": False,
                  "cached_nodeclaim_name": nodeclaim_name,
                  "cached_node_name": "N/A",
                  "cached_instance_id": "N/A",
                  "cached_node_ip_address": "N/A"
              })
              
              karpenter_logger.debug(f"Processing NodeClaim event. Type: {original_event_type}, NodeClaim: {nodeclaim_name}, Ready: {is_ready}",
                                    nodeclaim_name=nodeclaim_name,
                                    node_name=node_name_from_nodeclaim,
                                    instance_id=instance_id_from_nodeclaim,
                                    node_ip_address="N/A",
                                    event_type=original_event_type,
                                    extra={"event_status": "debug_event_received", "scale_action": "N/A"})
              
              
              if original_event_type == 'MODIFIED':
                if is_ready and not current_node_state["is_ready"]:
                  actual_node_ip_address = get_node_info(core_api, node_name_from_nodeclaim)
                  
                  if node_name_from_nodeclaim != "N/A" and \
                      instance_id_from_nodeclaim != "N/A" and \
                      actual_node_ip_address != "N/A":
                    
                    # スケールアウト完了ログ (NodeがReadyになり、情報が揃った場合)
                    current_node_state["is_ready"] = True
                    current_node_state["cached_node_name"] = node_name_from_nodeclaim
                    current_node_state["cached_instance_id"] = instance_id_from_nodeclaim
                    current_node_state["cached_node_ip_address"] = actual_node_ip_address
                    last_logged_node_states[nodeclaim_name] = current_node_state
                    
                    karpenter_logger.info(f"Node is READY and joined the cluster. Scale Out completed.",
                                          nodeclaim_name=current_node_state["cached_nodeclaim_name"],
                                          node_name=current_node_state["cached_node_name"],
                                          instance_id=current_node_state["cached_instance_id"],
                                          node_ip_address=current_node_state["cached_node_ip_address"],
                                          event_type=original_event_type,
                                          extra={"event_status": "node_ready", "scale_action": "ScaleOut"})
                  else:
                    current_node_state["is_ready"] = True 
                    last_logged_node_states[nodeclaim_name] = current_node_state
                
                elif provider_id and not current_node_state["cached_instance_id"] == instance_id_from_nodeclaim:
                  current_node_state["cached_instance_id"] = instance_id_from_nodeclaim
                  current_node_state["cached_node_name"] = node_name_from_nodeclaim
                  last_logged_node_states[nodeclaim_name] = current_node_state
                  
                  karpenter_logger.debug(f"NodeClaim providerID updated to: {instance_id_from_nodeclaim}.",
                                         nodeclaim_name=current_node_state["cached_nodeclaim_name"],
                                         node_name=current_node_state["cached_node_name"],
                                         instance_id=current_node_state["cached_instance_id"],
                                         node_ip_address=current_node_state["cached_node_ip_address"],
                                         event_type=original_event_type,
                                         extra={"event_status": "debug_provider_id_set", "scale_action": "N/A"})
                else:
                  karpenter_logger.debug(f"NodeClaim MODIFIED event with no significant state change. NodeClaim: {nodeclaim_name}",
                                         nodeclaim_name=nodeclaim_name,
                                         node_name=node_name_from_nodeclaim,
                                         instance_id=instance_id_from_nodeclaim,
                                         node_ip_address="N/A",
                                         event_type=original_event_type,
                                         extra={"event_status": "debug_modified_noop", "scale_action": "N/A"})
              
              elif original_event_type == 'DELETED':
                if nodeclaim_name in last_logged_node_states and not current_node_state["deleted_logged"]:
                  # スケールイン完了ログ (NodeClaimが削除された場合)
                  karpenter_logger.info(f"NodeClaim DELETED. Node has been terminated. Scale In completed.",
                                        nodeclaim_name=current_node_state["cached_nodeclaim_name"],
                                        node_name=current_node_state["cached_node_name"],
                                        instance_id=current_node_state["cached_instance_id"],
                                        node_ip_address=current_node_state["cached_node_ip_address"],
                                        event_type=original_event_type,
                                        extra={"event_status": "scale_in_terminated", "scale_action": "ScaleIn"})
                  current_node_state["deleted_logged"] = True
                  del last_logged_node_states[nodeclaim_name]
                else:
                  logged_name = last_logged_node_states.get(nodeclaim_name, {}).get("cached_nodeclaim_name", nodeclaim_name)
                  karpenter_logger.debug(f"NodeClaim DELETED event received, but already logged or no cache for {logged_name}.",
                                         nodeclaim_name=logged_name,
                                         node_name="N/A",
                                         instance_id="N/A",
                                         node_ip_address="N/A",
                                         event_type=original_event_type,
                                         extra={"event_status": "debug_deleted_noop", "scale_action": "N/A"})
              else:
                karpenter_logger.debug(f"Unhandled NodeClaim Event type: {original_event_type}. NodeClaim: {nodeclaim_name}",
                                       nodeclaim_name=nodeclaim_name,
                                       node_name=node_name_from_nodeclaim,
                                       instance_id=instance_id_from_nodeclaim,
                                       node_ip_address="N/A",
                                       event_type=original_event_type,
                                       extra={"event_status": "debug_unhandled_event", "scale_action": "N/A"})
                               
          if __name__ == "__main__":
            logging_karpenter_node_scale()
          EOF_PYTHON
          python /app/karpenter-scale-logger.py
        env:
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: karpenter-scale-logger-config
              key: LOG_LEVEL
        - name: ENV
          valueFrom:
            configMapKeyRef:
              name: karpenter-scale-logger-config
              key: ENV
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
EOF

#==========================================================
# Verify Pod
#==========================================================

kubectl get pods -n karpenter-scale-logger

kubectl logs -n karpenter-scale-logger $(kubectl get po -n karpenter-scale-logger -o jsonpath={.items[0].metadata.name}) -f

#==========================================================
# CleanUP
#==========================================================

kubectl delete deployment karpenter-node-logger -n karpenter-node-logger
kubectl delete ClusterRoleBinding karpenter-node-logger-binding
kubectl delete ClusterRole karpenter-node-logger -n karpenter-node-logger
kubectl delete ServiceAccount karpenter-node-logger -n karpenter-node-logger
kubectl delete Namespace karpenter-node-logger

